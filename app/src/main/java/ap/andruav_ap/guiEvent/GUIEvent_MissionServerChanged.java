package ap.andruav_ap.guiEvent;

/***
 * Posted by {@link ap.andruav_ap.activities.main.HubConnectionSheet} after the Mission Server
 * (Andruav HUB) settings - server IP/port, Andruav Cloud Server toggle - are saved, so the
 * Home screen can refresh its Mission Server card even though the sheet does not hold a
 * reference back to it.
 */
public class GUIEvent_MissionServerChanged {
}
